package com.savestate.backend.shared;

/**
 * Trophy/achievement data sources. Nintendo is intentionally absent — the
 * Switch has no platform-wide achievement system to sync (see root README).
 */
public enum Provider {
    STEAM,
    XBOX,
    PSN
}
